---
"@openruntime/extension-mf": minor
---

Add navigation-time Module Federation observability injection, the multi-instance-aware `mf status` and `mf module-info` commands, and a reusable public API for MF readers, selectors, and result builders.
