---
"@divebell/extension-mf": minor
---

Add navigation-time Module Federation observability injection, deterministic syncing from the public Observability Plugin browser entry with source metadata, the multi-instance-aware `mf status` and `mf module-info` commands, and a reusable public API for Remote, Shared, Bridge, selectors, and result builders.
