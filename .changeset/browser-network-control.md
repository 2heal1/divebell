---
"@divebell/cli": minor
---

Add structured Extension-provided conditional proxy PAC configuration and CDP-based HTTP(S) request rewrite and fulfill controls for Divebell-launched Chromium sessions.
