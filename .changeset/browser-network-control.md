---
"@divebell/cli": minor
---

Add fixed proxy and direct PAC URL configuration plus CDP-based HTTP(S) request rewrite and fulfill controls for Divebell-launched Chromium sessions. Synthetic request redirects remain intentionally unsupported because reliable browser follow-up navigation was not verified.
