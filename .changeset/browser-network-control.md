---
"@divebell/cli": minor
---

Add structured Extension-provided conditional proxy PAC configuration and CDP-based HTTP(S) request rewrite and fulfill controls for Divebell-launched Chromium sessions. Synthetic request redirects are intentionally not exposed because reliable browser follow-up navigation was not verified.
