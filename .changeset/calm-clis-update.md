---
"@divebell/cli": minor
---

Add a configurable built-in CLI updater, a top-level `divebell update` command,
and non-blocking periodic background upgrades for the npm global installation.
Branded CLI wrappers can provide their own updater without modeling package
management as an Extension.
