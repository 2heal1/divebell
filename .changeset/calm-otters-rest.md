---
"@divebell/cli": patch
---

Preserve the browser restore mode selected by `open --state`, `open --profile`, or `open --allowed-domains` across later page and Extension commands so the shared browser session is not relaunched with automatic restore enabled.
