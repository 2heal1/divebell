---
"@divebell/extension-mf": patch
---

Retain early ReactDOM renderer registrations when development React is enabled so a local remote's later React Refresh runtime can update components rendered by the production host.
