---
"@divebell/cli": minor
---

Give each opened page a dedicated automatically assigned Bridge port, remember that Bridge and browser session by working directory, and select the first registered Runtime by default. Use `stop` as the only page shutdown command while retaining Extension cleanup hooks, including cleanup before a page is replaced by another `open`.
