---
"@openruntime/extension-mf": minor
---

Add reusable, multi-instance Bridge lifecycle analysis and the `openruntime mf bridge trace` command with operation-level consumer and producer correlation, conservative capability handling, safe route and error output, copyable candidate commands, and explicit application-readiness boundaries.
