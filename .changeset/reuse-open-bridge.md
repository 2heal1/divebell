---
"@divebell/cli": patch
---

Reuse the current working directory's Bridge and user initialization scripts across repeated `open` commands so the browser can navigate without relaunching at a blank page.
