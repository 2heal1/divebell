---
"@divebell/cli": patch
---

Bundle the main Divebell Skill in the CLI package and add `divebell skill` to print its local path. Clarify that Extension Skills remain available only through their individual `divebell <command> --skill` lookups.
