---
"@divebell/cli": patch
---

Enable WebMCP for local Chrome launches by default, add `webmcp list` and `webmcp call` CLI commands, and expose typed Extension browser APIs for discovering and invoking page-registered tools. External and unsupported browsers remain usable until a WebMCP command is requested.
