{
  "name": "@divebell/demo-quickstart",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "rsbuild dev",
    "build": "pnpm --dir ../../packages/core build && pnpm --dir ../../packages/chunk-map build && pnpm --dir ../../packages/rspack-plugin build && rsbuild build",
    "build:pages": "DIVEBELL_PAGES_BASE=/divebell/quickstart/ pnpm run build",
    "preview": "rsbuild preview",
    "verify:build": "node ./scripts/verify-build.mjs"
  },
  "dependencies": {
    "@divebell/core": "workspace:^"
  },
  "devDependencies": {
    "@divebell/rspack-plugin": "workspace:^",
    "@rsbuild/core": "2.0.11"
  }
}
