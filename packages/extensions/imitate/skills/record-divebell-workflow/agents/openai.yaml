interface:
  display_name: "Record a Divebell workflow"
  short_description: "Record web actions and elements, then generate and verify an executable JavaScript replay"
  default_prompt: "Use $record-divebell-workflow to open a browser, record operated elements and event order, automatically attempt optional audio capture, and generate and verify an executable JavaScript replay. Ignore missing audio."
