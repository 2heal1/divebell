interface:
  display_name: "Record a Divebell workflow"
  short_description: "Record and verify a browser workflow"
  default_prompt: "Use $record-divebell-workflow to record, review, amend, and verify my browser workflow with the latest Chrome Profile unless I specify another context."
